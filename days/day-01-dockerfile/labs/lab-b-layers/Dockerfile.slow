# Slow rebuild: COPY everything before npm install so any file change busts the cache.
FROM node:22-alpine
WORKDIR /app
COPY . .
RUN npm install --omit=dev || true
CMD ["node", "-e", "console.log('lab-b slow')"]
