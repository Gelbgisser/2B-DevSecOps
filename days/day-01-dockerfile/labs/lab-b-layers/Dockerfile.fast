# Fast rebuild: lockfiles first so source edits reuse the npm layer.
FROM node:22-alpine
WORKDIR /app
COPY package.json ./
RUN npm install --omit=dev || true
COPY . .
CMD ["node", "-e", "console.log('lab-b fast')"]
