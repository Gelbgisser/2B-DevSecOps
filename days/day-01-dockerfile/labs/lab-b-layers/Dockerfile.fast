# FAST cache: install depends only on package.json. Editing source reuses this layer.
FROM node:22-alpine
WORKDIR /app
COPY package.json ./
RUN npm install --omit=dev || true
COPY . .
CMD ["node", "-e", "console.log('lab-b fast')"]
