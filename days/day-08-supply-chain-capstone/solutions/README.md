# Day 8 solutions

Grype DB: set `GRYPE_DB_CACHE_DIR`, or copy the DB tarball from a connected machine. Do not disable `--fail-on critical` to “go green”.

Gitleaks on this repo should be clean; if it flags `.env.example` lab strings, tune the allowlist **in class discussion**, not by deleting the tool.
