#!/usr/bin/env node
// Stream the EICAR test string to stdout. Do NOT write eicar.com to disk —
// Windows Defender will quarantine the lab files.
process.stdout.write(
  "X5O!P%@AP[4\\PZX54(P^)7CC)7}$EICAR-STANDARD-ANTIVIRUS-TEST-FILE!$H+H*"
);
